# filename: docker/airflow/requirements.txt
# purpose:  Python packages installed on top of apache/airflow:2.8.4-python3.11
#           for DAG tasks (predict_pipeline, feature_store, drift_monitor)
# version:  1.0

# Core ML
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.0.3
lightgbm==4.3.0
joblib==1.3.2

# MLflow (model registry + artifact loading)
# Pin SQLAlchemy < 2.0 — mlflow 2.14.1 pulls in 2.x which breaks Airflow 2.8.4's
# psycopg2 dialect (executemany_mode=values was removed in SQLAlchemy 2.0)
mlflow==2.14.1
SQLAlchemy>=1.4.36,<2.0

# Feature store
redis==5.0.1

# Drift monitoring
evidently==0.4.30

# Utilities
python-dotenv==1.0.1
psycopg2-binary==2.9.9
