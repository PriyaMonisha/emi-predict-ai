global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "emi-api"
    static_configs:
      # Inside Docker Compose: use service name, not host.docker.internal
      - targets: ["fastapi:8000"]
    metrics_path: /metrics
    scrape_interval: 10s
